Django==6.0.6
python-dotenv==1.0.1
requests==2.32.5
reportlab==5.0.0
Pillow==12.2.0

# --- Déploiement sur Render ---
gunicorn==23.0.0        # serveur WSGI de production
whitenoise==6.8.2       # sert les fichiers statiques directement depuis Django
dj-database-url==2.3.0  # parse la variable DATABASE_URL (Postgres fourni par Render)
psycopg==3.2.10         # driver PostgreSQL (v3, remplace psycopg2)
psycopg-binary==3.2.10  # binaires précompilés du driver (pas de compilation nécessaire au déploiement)
