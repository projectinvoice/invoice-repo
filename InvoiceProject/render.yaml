databases:
  - name: invoiceproject2-db
    plan: free
    databaseName: invoiceproject2
    user: invoiceproject2

services:
  - type: web
    name: invoiceproject2
    runtime: python
    plan: free
    buildCommand: "./build.sh"
    startCommand: "gunicorn InvoiceProject.wsgi:application --bind 0.0.0.0:$PORT"
    envVars:
      - key: PYTHON_VERSION
        value: 3.12.3
      - key: DEBUG
        value: "False"
      - key: SECRET_KEY
        generateValue: true
      - key: DATABASE_URL
        fromDatabase:
          name: invoiceproject2-db
          property: connectionString
      # À compléter manuellement dans le dashboard Render après le premier déploiement
      # (Render vous donne l'URL finale, ex: invoiceproject2.onrender.com) :
      - key: ALLOWED_HOSTS
        sync: false
      - key: CSRF_TRUSTED_ORIGINS
        sync: false
      # Facultatif — laissez vide pour désactiver l'assistant IA
      - key: GEMINI_API_KEY
        sync: false
      - key: GEMINI_MODEL
        value: gemini-2.5-flash
      # Facultatif — paiement des abonnements (MoneyFusion)
      - key: MONEYFUSION_API_URL
        sync: false
      - key: SITE_BASE_URL
        sync: false
      # Facultatif — envoi d'e-mails réels (sinon affichés dans les logs)
      - key: EMAIL_BACKEND
        value: django.core.mail.backends.console.EmailBackend
